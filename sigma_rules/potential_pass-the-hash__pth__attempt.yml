title: Potential Pass-the-Hash (PtH) Attempt
id: daafdf96-e7b1-4f14-b494-27e0d24b11f6
status: stable
description: Adversaries may pass the hash using stolen password hashes to move laterally
  within an environment, bypassing normal system access controls. Pass the hash (PtH)
  is a method of authenticating as a user without having access to the user's cleartext
  password.
author:
- Elastic
date: '2025-12-09'
modified: '2025-12-05'
references:
- https://attack.mitre.org/techniques/T1550/002/
tags:
- attack.ta0008
- 'Data Source: Windows Security Event Logs'
- attack.t1550
- 'Tactic: Lateral Movement'
- attack.lateral_movement
- 'Use Case: Threat Detection'
- 'OS: Windows'
- 'Domain: Endpoint'
- 'Resources: Investigation Guide'
level: medium
logsource:
  category: process_creation
  product: windows
  index:
  - winlogbeat-*
  - logs-windows.forwarded*
  - logs-system.security*
detection:
  selection:
    query: 'host.os.type:"windows" and

      event.category : "authentication" and event.action : "logged-in" and

      winlog.logon.type : "NewCredentials" and event.outcome : "success" and

      user.id : (S-1-5-21-* or S-1-12-1-*) and winlog.event_data.LogonProcessName
      : "seclogo"

      '
  condition: selection
falsepositives:
- Unknown
fields:
- event.action
- event.category
- event.outcome
- host.os.type
- user.id
- winlog.event_data.LogonProcessName
- winlog.logon.type
